using System;
using UnityEngine;

public enum Rarity
{
    Common,
    Rare,
    Epic
}

public enum TargetType
{
    Tower = 1,
    Monster
}

public enum StatType
{
    Attack,
    AttackSpeed,
    Speed,
    Health
}
